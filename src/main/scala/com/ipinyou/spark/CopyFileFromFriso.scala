package com.ipinyou.spark

import com.ipinyou.common.FTPToHdfs
import org.apache.spark.sql.SparkSession


class CopyFileFromFriso {
/*  def copyFtpToHdfs(spark:SparkSession, dateStr:String): Boolean ={
    val bool: Boolean = FTPToHdfs.FTPCheck(dateStr)
    if(!bool){
      false
    }
    //    ftpPath: String, ftpFile: String, localPath: String, hdfsPath: String
    var mobilePath=s"Friso/$dateStr/click/mobile"
    var pcPath=s"Friso/$dateStr/click/pc"
    var otvPath=s"Friso/$dateStr/click/otv"
    var ftpPath=s"Friso/$dateStr/view/mobile"
    var ftpPath=s"Friso/$dateStr/click/mobile"


  }*/

}
